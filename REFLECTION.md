# 反思报告

## 一、从想法到工程项目

这次项目让我经历了从模糊想法到工程交付的过程。最开始我只确定要做“第一个反馈闭环”，还没有想清楚如何落地。brainstorming 给出了多种方案，也提出了我原本忽略的权限边界、计划审批、危险操作确认和失败反馈。这促使我重新思考：LLM 只能提出结构化动作，执行权和安全策略必须由项目代码掌握。

brainstorming 是对我帮助最大的技能之一，它扩大了方案空间。subagent-driven-development 在任务范围、接口和验收条件明确时效率较高，适合完成完整功能。按函数切分需要过细的接口文档，也容易失去整体性；按功能拆分更符合模块封装和统一接口。本项目的冷启动 agent 曾因工具和网关问题无法完成测试与提交，后续也有任务由主代理接管，因此不能断言它能长期自主运行。更准确的结论是：边界清楚时效率高，但工具失败时仍需介入。

## 二、TDD 是约束，也是放大器

本项目总体坚持 RED、GREEN、REFACTOR；过程中曾出现两次实现先于对应 RED 的临时偏离，但都在提交前撤回并重新观察了失败。TDD 确实延长了开发时间，但总体上是 AI 协作的放大器。AI 很容易在写完代码后根据自己的实现解释需求，或者在没有证据时认为任务完成。先写失败测试，可以证明行为确实缺失，再让实现针对缺口推进，降低偏离风险。

GitHub Actions 契约是一个具体例子。测试先确认 workflow 不存在并得到 RED，实现 CI 后才 GREEN。独立审查发现，早期测试只检查命令字符串，把命令放进 `echo` 也可能通过。受控变异重新得到 RED 后，测试被加强为精确检查 action、参数和步骤顺序。Render、Docker 和 GHCR 契约则先暴露 `render.yaml`、动态端口和 publish job 缺失，实现后测试才通过。这说明测试闭环也在检验测试本身。

虽然开发阶段为此付出了更多时间，但我认为完备的测试过程是必要的，成本可以接受。“有证据才算完成”也是一种保障。要确保项目真正落地，就必须让测试、审查、构建、部署和公网验证各个环节都有可核对的结果，而不能只以代码已经写完作为完成标准。

## 三、计划、任务拆分与上下文恢复

SPEC 和 PLAN 的质量会直接影响后续执行。冷启动时，陌生 agent 仅依据早期文档就暴露出状态门、Git root、动作模型和敏感删除优先级描述不够明确的问题，后来才修正规约。这是比外部平台变化更直接的“规约不清导致执行偏差”案例。好的计划首先要明确目标和边界，其次要有异常处理预案，而不能只描述理想路径。本项目后来又遇到 Render 信用卡限制，只能改用阿里云 VPS，说明计划还应准备备选方案，让目标不变而路径可以调整。

任务过大时，上下文容易混乱。如果让同一个 agent 同时处理代码、CI、容器发布、云服务器和文档，尤其在 context compact 后，很容易记错目录、版本或状态。因此我倾向于按完整功能切分，并在提交、失败、外部操作和危险动作前介入。

本项目中最有效的 context 策略不是给模型更多历史对话，而是让进度落实到计划、日志、Git 历史和任务台账。context compact 后的记忆可能模糊甚至混淆多个版本，可靠的恢复方式应当是从当前仓库状态开始溯源，依次确认 worktree、branch、HEAD、测试证据、部署状态和下一步，而不是依赖模型“记得大概做到了哪里”。如果重新做一次，我会更早设计这种恢复机制，使每次恢复都从文档化状态出发。

## 四、从本地演示到真实交付

凭据和分发要求让我认识到“代码完成”和“工程交付完成”的差距。我原本熟悉 key 不进配置、数据库和日志，keyring、不可逆指纹和查看时不回显也不是新知识；但过去没有把“LLM 不能直接获得 shell、文件和凭据权限”落实好。这次才明确执行权必须经过确定性策略层。分发方面，我以前只在内网或 localhost 展示 demo，没有真正考虑公开镜像、动态端口、安全组、重启策略、版本对应、回滚和公网扫描。服务器连接 GHCR 超时后，改用 GitHub Actions 已 smoke 的同一 artifact，经双重 SHA-256 校验后上传。这些问题在本地 demo 中容易被忽略，但真实交付不能回避。

公开服务也不能只验证首页能打开。项目最终还验证了确定性 mock 场景、凭据写入返回 403 且不回显密钥、本地仓库路径任务返回 400，并观察到互联网上对 `/.env.local` 等路径的自动扫描得到 404。这个过程让我意识到，部署不仅是让进程启动，还包括版本可追溯、安全边界、运行状态和异常恢复。

## 五、对 Superpowers 的批判与改进

Superpowers 最大的问题是流程过重、对环境的假设过于理想。它默认模型、subagent、Git、shell、Docker、网络和云平台都能稳定工作，也假设计划可以提前写得足够确定、长上下文可以可靠维持状态。但本项目实际遇到了模型不可用、代理连接失败、本机没有 Docker、SSH 兼容问题、Render 认证限制和 GHCR 网络超时。普通请求也严格走完整框架，加上每一步都需要批准时，效率会明显下降。流程本身不算无法接受，但使用门槛和逐步审批速度确实需要改善。

如果重新做，我会把任务按可独立验收的功能切得更清楚，使工作树和模块来源更容易追踪；同时设计批量验收机制，把一组范围明确、低风险、可回滚的操作一次批准，而危险操作仍单独审批。我也会把 context compact 恢复机制作为项目流程的一部分，强制从文档和当前状态恢复上下文。

我认为 Superpowers 最值得保留的是 TDD、独立审查和“有证据才算完成”。这些纪律确实降低了 AI 偏离和过早宣称完成的风险。但它更适合大型、长期或高风险工程，普通任务应根据风险简化流程，而不是全部照搬。更理想的方法不是放弃工程纪律，而是在保留关键门禁的同时，使审批、计划深度和审查强度能够随任务风险调整。

---

说明：本文观点和项目体验由学生本人提供，AI 仅协助整理结构、核对项目事实和语言润色。
